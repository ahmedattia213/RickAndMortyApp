import SwiftUI

/// A utility class for configuring the appearance of the app.
final class Appearance {
    
    /// Call this method to set up the app's appearance
    static func setup() {
        // for instance
        // UIRefreshControl.appearance().tintColor = UIColor(Color.textPrimary)
    }
}
